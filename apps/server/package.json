{
  "name": "@chat/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsup src/index.ts --format esm --platform node --out-dir dist --clean",
    "start": "node dist/index.js",
    "test": "vitest run src/room/roomPolicy.test.ts src/network/corsPolicy.test.ts src/network/portGuard.test.ts src/audit/JsonlAuditSink.test.ts"
  },
  "dependencies": {
    "@chat/core": "workspace:*",
    "@chat/protocol": "workspace:*",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "socket.io": "^4.8.1",
    "zod": "^4.1.12"
  }
}
